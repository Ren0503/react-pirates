import useClickOutside from '.'

describe('useClickOutside', () => {
  it('is truthy', () => {
    expect(useClickOutside).toBeTruthy()
  })

  it('should be defined', () => {
    expect(useClickOutside).toBeDefined()
  })
})
