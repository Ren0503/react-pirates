import useBodyScrollLock from '.'

describe('useBodyScrollLock', () => {
  it('is truthy', () => {
    expect(useBodyScrollLock).toBeTruthy()
  })

  it('should be defined', () => {
    expect(useBodyScrollLock).toBeDefined()
  })
})
